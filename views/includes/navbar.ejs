<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/listings"><i class="bx bx-compass"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav">
                <a class="nav-link" href="/listings">Expolore</a>
            </div>
            <div class="navbar-nav ms-auto">
                <form class="d-flex search-form" role="search" action="/listings" method="get">
                    <input class="form-control me-2" type="search" name="search" value="<%= search || '' %>" placeholder="Search listings" aria-label="Search listings" />
                    <button class="btn btn-search" type="submit">Search</button>
                </form>
            </div>
            <div class="navbar-nav ms-auto mr-5">
                <a class="nav-link" href="/listings/new">Add New Listing</a>
                <% if(!userStatus){ %>
                    <a class="nav-link" aria-current="page" href="/signup"><b>Sign Up</b></a>
                    <a class="nav-link" href="/login?redirect=<%= encodeURIComponent(currentUrl) %>"><b>Log in</b></a>
                    <% } %>
                        <% if(userStatus){ %>
                            <a class="nav-link" href="/logout"><b>Log Out</b></a>
                            <% } %>
            </div>
        </div>
    </div>
</nav>